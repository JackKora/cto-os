# cto-os-data

Personal state for CTO OS. See `CLAUDE.md` (or its `AGENTS.md` symlink) for orientation.

Logic lives in the skill repo, registered at `~/.claude/skills/cto-os/` for Claude and `~/.agents/skills/cto-os/` for Codex. Both links target the same checkout. This directory holds only your data.
